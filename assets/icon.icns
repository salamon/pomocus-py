# Placeholder for macOS icon
# Replace with actual .icns file
# You can create .icns files using:
# 1. macOS built-in iconutil (requires PNG files in multiple sizes)
# 2. Online converters from PNG to ICNS
# 3. Image editing tools like GIMP with macOS plugins
